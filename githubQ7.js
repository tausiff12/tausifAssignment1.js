const operatingSystem = {
    Name : "Ubuntu" ,
    version : 18.4 ,
    license : "open source"
}

// console.log(operatingSystem);

const entries = Object.entries(operatingSystem);
console.log(entries);